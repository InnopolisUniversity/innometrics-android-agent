<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_upload_local_data"
        android:title="@string/action_upload_local_data_str"
        android:icon="@drawable/ic_cloud_upload"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_clear_local_data"
        android:title="@string/action_delete_local_data_str"
        android:icon="@drawable/ic_delete_sweep"
        app:showAsAction="ifRoom"/>

</menu>